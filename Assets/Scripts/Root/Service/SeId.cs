namespace Root.Service
{
    // 追加する際は必ず末尾に
    public enum SeId
    {
        /// SE なしを表す番兵値
        None = 0,
        Click,
        ShopPurchase,
        RoomPick,
        RoomPlace,
        RoomStow,
        OutfitEquip,
        Back,
    }
}
